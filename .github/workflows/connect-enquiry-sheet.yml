name: Connect enquiry form to Google Sheet (retired)

on:
  workflow_dispatch:

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy one-time patch retired; the live enquiry form is already connected."
