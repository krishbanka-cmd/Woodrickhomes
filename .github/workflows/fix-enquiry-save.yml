name: Enquiry save patch (retired)

on:
  workflow_dispatch:

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy one-time patch retired; the live enquiry workflow remains unchanged."
